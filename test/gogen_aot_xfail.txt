# Shrink-only allowlist of test/gold-aot/*.lg fixtures that currently
# DIVERGE between the bytecode and -tags gogen_ir engines.
# Managed by TestGogenAOTDiff: re-seed with `LETGO_AOT_REDERIVE=1 go test -run TestGogenAOTDiff .`.
# A new divergence not listed here fails CI; a listed fixture that starts
# agreeing also fails (remove it). The list must only ever shrink.
